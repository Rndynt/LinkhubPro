# Linkhub Pro - API Documentation

Base URL: `https://your-domain.netlify.app/.netlify/functions`

## 🔐 Authentication

All authenticated endpoints require a Bearer token in the Authorization header:

```http
Authorization: Bearer <your_jwt_token>
